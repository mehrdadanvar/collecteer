# Hello, world!

<div className="font-serif">
  > Some notable things in a block quote!

```js
let sample = [1, 2, 3];
const data = await axios.get("https://api.astro.com/v1");
```

```python
print("hello world")
def mehrdad():
  return [x for x in range(10)]
```

</div>
